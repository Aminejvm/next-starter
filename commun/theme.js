import colors from "./themeDependencies/colors";
import { space, shadows, borders } from "./themeDependencies/styles";
import { fontSizes } from "./themeDependencies/typography";

const theme = {
  colors,
  space,
  shadows,
  borders,
  fontSizes
};

export default theme;
